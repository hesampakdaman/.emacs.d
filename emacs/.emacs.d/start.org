* Global settings
It is of most important that this file is loaded first. Here we define
global variables, such as leader keys and theme-colors, that are
assumed to exists in other configuration files.
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "global.org" user-emacs-directory))
#+END_SRC

* General settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "general.org" user-emacs-directory))
#+END_SRC

* Applications settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "apps.org" user-emacs-directory))
#+END_SRC

* Completion settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "completion.org" user-emacs-directory))
#+END_SRC

* Editing settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "editing.org" user-emacs-directory))
#+END_SRC

* Keybindings settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "keybindings.org" user-emacs-directory))
#+END_SRC
* Miscellaneous Packages
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "misc-packages.org" user-emacs-directory))
#+END_SRC

* Org mode settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "org-mode.org" user-emacs-directory))
#+END_SRC

* Programming settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "programming.org" user-emacs-directory))
#+END_SRC

* Search settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "search.org" user-emacs-directory))
#+END_SRC

* Shell settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "shell.org" user-emacs-directory))
#+END_SRC

* UI Enhancement settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "ui-enhancement.org" user-emacs-directory))
#+END_SRC

* Window settings
#+BEGIN_SRC emacs-lisp
  (org-babel-load-file
   (expand-file-name "window.org" user-emacs-directory))
#+END_SRC


